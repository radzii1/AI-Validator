import httpx

BASE = "http://127.0.0.1:8000"

def chat(msg: str):
    r = httpx.post(f"{BASE}/chat", json={"message": msg})
    r.raise_for_status()
    return r.json()

def similar(a: str, b: str) -> bool:
    # ultra-basic matcher for deterministic mock
    return a.strip() == b.strip()

